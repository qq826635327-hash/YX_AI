# 10 · Phase 路线图与重构计划

> 项目后续要怎么走。当前在哪个阶段、还有什么没做。
> **与 PRD-30 保持同步**，详细里程碑和验收标准见 `doc/PRD/30-路线图与验收标准.md`。

## 1. 当前定位

- **架构阶段**：Web 为主，FastAPI + React 18 单体应用
- **核心能力**：项目 → 剧本 → 解析 → 生成 → 资产库
- **AI 集成**：Agnes（已用）、ComfyUI（占位）
- **持久化**：SQLite（生产应换 PostgreSQL）
- **任务队列**：asyncio.Semaphore + create_task（当前使用，已移除 Huey）

## 2. Phase 路线

### Phase 1 · MVP ✅ 已完成

**目标**：跑通"输入剧本 → 生成图片"的核心闭环。

| 模块 | 状态 |
| --- | --- |
| 项目/剧本/角色/场景/道具/剧集/分镜 CRUD | ✓ |
| 通用 CRUD 抽象 | ✓（2026-06-20 重构） |
| 角色 gender/age 字段 | ✓（2026-06-22 新增） |
| 场景 camera_hint 字段 | ✓（2026-06-22 新增） |
| 分镜镜头参数（camera_size/angle/movement） | ✓（2026-06-22 新增） |
| 项目画风预置（style_preset） | ✓（2026-06-22 新增） |
| 分镜7段提示词（首帧/尾帧/视频独立输出） | ✓（2026-06-22 新增） |
| 分镜拖拽排序 + 自动编号 | ✓（2026-06-22 新增） |
| 画风传递链（项目→模板→LLM） | ✓（2026-06-22 新增） |
| LLM 剧本解析 | ✓（三阶段管线：提取→章节→分镜；7段分镜格式；画风注入） |
| 图片生成（Agnes） | ✓ |
| 自动回填图片到实体 | ✓ |
| 任务中心 | ✓ |
| 资产库 | ✓ |
| 任务进度 WebSocket | ✓ |
| Provider 管理 | ✓ |
| ComfyUI 管理工作流 | ✓（UI）；执行器 ⚠️ |
| 浮动日志查看器 | ✓（2026-06-20 新增） |
| HTTP 错误日志 | ✓（2026-06-20 新增） |
| 全局错误捕获 | ✓（2026-06-20 新增） |
| 前端体验优化（部分） | ✓ |
| 角色卡片显示性别·年龄 | ✓（2026-06-22 新增） |
| 删除图片 bug 修复 | ✓（2026-06-20） |
| 通用 CRUD 重构 | ✓（2026-06-20） |
| 全局代码优化 | ✓（2026-06-20：重复代码消除、session 合并、异步化、路由修复等） |

### Phase 2 · 结构化升级 ✅ 已完成

**目标**：AI 解析增强、画风预置、分镜编排优化、文档体系完善。

| 模块 | 状态 |
| --- | --- |
| 3 阶段 AI 解析（提取→章节→分镜） | ✓（2026-06-21） |
| 画风预设 + 传递链 | ✓（2026-06-22） |
| 7 段分镜提示词 | ✓（2026-06-22） |
| 分镜拖拽排序 + 自动编号 | ✓（2026-06-22） |
| 文档体系（PRD + 开发手册 + 测试手册） | ✓（2026-06-22） |

### Phase 3 · 真实生成引擎 🚧 进行中

**目标**：Provider 抽象落地、视频生成、运维基础设施、代码重构。

| 模块 | 状态 |
| --- | --- |
| ProviderHandler 基类 + translate/parse 模式 | ✓（2026-06-22） |
| Agnes Image 2.1 Flash handler | ✓（2026-06-22） |
| Agnes Video V2.0 集成 | ✓（2026-06-21） |
| 参考图注入（4 种预设） | ✓（2026-06-21） |
| 模型能力数据迁移（param_specs + capabilities → DB） | ✓（2026-06-22） |
| 数据库备份 + 保留策略 + WAL 检查点 | ✓（2026-06-23） |
| 优雅关闭 + 信号量重置 | ✓（2026-06-23） |
| 智能降级（fallback_provider） | ✓（2026-06-23） |
| Prompt 缓存（cache_key） | ✓（2026-06-23） |
| 成本追踪（output_payload） | ✓（2026-06-23） |
| script_parser 重构（拆分为 3 个模块） | ✓（2026-06-23） |
| 前端组件拆分（EpisodesPage 等） | ✓（2026-06-23） |
| Huey 移除 | ✓（2026-06-23） |
| vitest 测试基础设施（已集成，少量用例，持续补充） | ✓（2026-06-23） |
| ComfyUI 工作流执行器 | ⚠️ 进行中 |

**未完成（重要 TODO）**：
- ComfyUI 工作流执行器
- 任务取消（cancel API 接 asyncio）
- 任务详情页（TaskLog 列表）

### Phase 4 · 资产回流与多模态 📅 规划

**目标**：多模态资产关联、导出能力、缓存统计、成本仪表盘。

| 模块 | 计划 |
| --- | --- |
| 多模态资产关联 | 分镜→多角色/场景/道具图；资产反向引用 |
| 资产 backfill | 历史项目扫描、重复检测 |
| 导出能力 | 分镜表 PDF/HTML、视频合成 |
| Prompt 缓存命中率统计 | 按 Provider/模型/时间维度 |
| 多 Provider 负载均衡 | 权重/轮询/成本优先策略 |
| 成本趋势分析仪表盘 | 日/周/月统计、预算预警 |

### Phase 5 · 设置中心完善 📅 规划

**目标**：插件系统、主题系统、多语言。

| 模块 | 计划 |
| --- | --- |
| 插件系统 | 注册/卸载/沙箱执行/市场 |
| 主题系统 | 深色/浅色/系统跟随 |
| 多语言（i18n） | 中文 + 英文 |

## 3. 紧急 TODO（Phase 3 收尾）

按优先级：

1. **ComfyUI 执行器**（重要）—— 已有 UI，缺执行
2. **任务取消**（重要）—— 用户能停掉卡住的任务
3. **任务详情页**（次要）—— 展示 TaskLog

## 4. 架构演进建议

| 当前 | 建议演进 | 触发条件 |
| --- | --- | --- |
| FastAPI 单体 | FastAPI + 独立 worker 进程 | 任务耗时变长 |
| SQLite | PostgreSQL | 多人协作 |
| Vite + React SPA | Next.js（SSR + RSC） | SEO / 加载速度 |
| Tailwind 3 | Tailwind 4 | 升级 |
| Zustand | Zustand + Jotai（细粒度） | 状态复杂时 |
| WebSocket | SSE + WebSocket 混合 | 推送数据量大 |
| asyncio.Semaphore | Temporal / Celery | 任务流复杂 |

## 5. AI 工作的边界

**AI 应该做**：
- 加新功能（按"我要加新功能去哪改"清单）
- 修 Bug（按 `08-常见Bug与注意事项.md` 排查）
- 重构（在 `business_service` 抽象下加新实体）
- 写测试
- 写文档（必须同步更新本手册和 PRD）

**AI 不应该做**（除非用户明确要求）：
- 改数据库 schema（破坏性变更需要用户确认）
- 改 API 协议（前端要同步改）
- 改 WebSocket 协议（同上）
- 改 Provider 接口（影响所有 Provider 实现）
- 删除 Service / API 路由（要先 grep 看哪里在用）
- 改日志格式（会破坏历史 API 解析）
- 改通用 CRUD 函数签名（影响 5 个实体）

## 6. 文档维护规则

**强制规则**（用户要求）：

> 每次执行任务完成后，**必须**同步更新 `doc/开发手册/CHANGELOG.md` 和相关模块的 `doc/PRD/` 文档。

具体步骤：

1. **完成任务后**，在 `CHANGELOG.md` 加一条记录（日期、变更、影响范围）
2. **如修改了 API**：更新 `doc/PRD/22-API-与-WebSocket-契约.md`
3. **如修改了前端模块**：更新对应 `doc/PRD/XX-模块名.md`（PRD-10~16）
4. **如修改了开发相关内容**：更新 `doc/开发手册/XX-主题.md`（如 `02-后端架构.md`、`04-通用CRUD模式.md`）
5. **如发现新 Bug 或踩坑**：更新 `doc/开发手册/08-常见Bug与注意事项.md`
6. **如新增 TODO**：更新本章"紧急 TODO"段落
