# 任务记录 · 2026-06-22

## 任务 1：角色提示词字段映射修复

### 目标
修复剧本解析时角色 appearance 被错误赋给 description 而非 settings，导致生图调用描述字段而非提示词字段

### 结果
已修复：appearance→settings, description→description，与场景/道具一致

### 修改文件
- backend/app/pipelines/_extraction_stages.py
- backend/app/services/prompt_template_service.py

### 状态
已完成

---

## 任务 2：速率限制自动重试 + 智能降级开关 + 并发数可配置

### 目标
1. 剧本解析 LLM 调用增加速率限制自动重试（默认5次，65s等待）
2. 生图任务重试次数改为从配置读取
3. 智能降级增加开关控制
4. 任务中心并发数可配置
5. 系统状态页增加任务配置 UI

### 结果
全部完成，配置项支持运行时修改

### 修改文件
- backend/config.yaml — 增加 tasks.rate_limit_retry/rate_limit_wait/smart_fallback/max_concurrent
- backend/app/core/config.py — TasksConfig 增加新字段
- backend/app/pipelines/_llm_utils.py — _call_llm 和 _call_llm_stream 增加速率限制重试
- backend/app/tasks/execute_task.py — 重试参数从配置读取，智能降级受开关控制
- backend/app/api/generate.py — 并发数从配置读取
- backend/app/api/tasks.py — 引用 _get_max_concurrent 替代硬编码
- backend/app/api/settings.py — 增加 GET/PATCH /config/tasks
- frontend/src/types/index.ts — SystemConfig 增加 tasks 字段
- frontend/src/api/config.ts — 增加 updateTasks API
- frontend/src/pages/SystemStatusPage.tsx — 增加任务配置卡片

### 状态
已完成

---

## 任务 3：性能监控系统落地

### 目标
实现 AI 自动监控前端性能，出问题能更快定位原因：自动采集 longtask/paint/memory/API 耗时/全局错误，后端聚合并生成告警，前端弹窗实时展示。

### 结果
已完成：
- 前端 perf SDK：`mark` / `measure` / `count` + 自动观测 longtask / paint / memory
- 后端 `PerfSession` / `PerfEvent` / `PerfAlert` 三张表 + 聚合/告警服务
- `POST /api/perf/sessions`、`GET /api/perf/sessions`、`GET /api/perf/alerts`
- `/ws/perf` 实时告警推送
- `PerfMonitor.tsx` 弹窗：长任务/阻塞总时/内存、计数器、耗时分布、告警列表、历史会话、复制现场报告
- `TopBar` 新增「性能监控」入口
- `api/client.ts` 统一采集所有 API 请求耗时
- `main.tsx` 错误捕获联动 `perf.mark`

### 修改文件
- `backend/app/models/perf.py`（新建）
- `backend/app/schemas/perf.py`（新建）
- `backend/app/services/perf_service.py`（新建）
- `backend/app/api/perf.py`（新建）
- `backend/app/api/__init__.py` — 注册 perf 路由
- `backend/app/models/__init__.py` — 注册 perf 模型
- `backend/app/ws/routes.py` — 新增 `/ws/perf`
- `frontend/src/perf/` — SDK 目录（types/api/buffer/observer/index.ts，新建）
- `frontend/src/components/PerfMonitor.tsx`（新建）
- `frontend/src/components/layout/TopBar.tsx` — 添加入口
- `frontend/src/api/websocket.ts` — 新增 perfWs
- `frontend/src/api/client.ts` — API 耗时埋点
- `frontend/src/main.tsx` — 启动 perf、监听告警、错误 mark

### 状态
已完成 · 已验证通过

### 验证
- 后端 py_compile / import 验证通过
- 前端 `tsc --noEmit` 0 错误、`npm run build` 成功
- 前后端已重启，健康检查通过
- `/api/perf/sessions` 可正常访问
